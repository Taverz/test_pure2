import 'package:flutter/material.dart';

class BoardTab extends StatelessWidget {
  const BoardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Доска знакомств', style: TextStyle(fontSize: 24)),
    );
  }
}
